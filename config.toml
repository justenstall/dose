title = "oltdaniel"

# The URL the site will be built for
base_url = "https://justenstall"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# Explicitly set default language
default_language = "en"

# Enable taxonomies in default language
taxonomies = [
    { name = "tags" }
]

[languages.de]
# Enable taxonomies in german language
taxonomies = [
    { name = "tags" }
]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "visual-studio-dark"

[extra]
social_media = [
    { name = "LinkedIn", url = "https://mastodon.social/@Mastodon", rel = "me" },
    { name = "GitHub", url = "https://linkedin.com/in/justenstall" },
    { name = "Twitter", url = "https://twitter.com/@justenstall" },
    # { name = "Mastodon", url = "https://mastodon.social/@Mastodon", rel = "me" },
]
default_theme = "dark"